#include <iostream>
#define sq(a) (a)*(a)
using namespace std;
int main() {
	/*Preventing bootleneck in input*/
	ios::sync_with_stdio(0);
	cin.tie(0);
	double a, b;
	cin >> a >> b; 
	/*When the difference is so small, floats are equal*/
	if (abs(a - b) < 1e-9) {
		/*Both numbers are equal*/
		printf("%s %.9f \n", "Ambos numeros son iguales",1e-9);
	}
	/*Learning to use macros. a mere exercise*/
	cout << sq(a);
}
